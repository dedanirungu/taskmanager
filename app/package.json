{
  "name": "devplatform-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted developer task board + browser-VSCode platform",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js",
    "seed:admin": "node src/db/seed-admin.js",
    "build:frontend": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@fastify/cookie": "^9.4.0",
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/session": "^10.9.0",
    "@fastify/static": "^7.0.4",
    "bcryptjs": "^2.4.3",
    "dockerode": "^4.0.2",
    "fastify": "^4.28.1",
    "octokit": "^4.0.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
