name: devplatform

services:
  db:
    image: postgres:16-alpine
    container_name: devplatform-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${PGDATABASE:-devplatform}
      POSTGRES_USER: ${PGUSER:-devplatform}
      POSTGRES_PASSWORD: ${PGPASSWORD:?PGPASSWORD must be set in .env}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PGUSER:-devplatform}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: devplatform/app:latest
    container_name: devplatform-app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      PGHOST: db
      PGPORT: 5432
      PGDATABASE: ${PGDATABASE:-devplatform}
      PGUSER: ${PGUSER:-devplatform}
      PGPASSWORD: ${PGPASSWORD}
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET must be set}
      GITHUB_TOKEN: ${GITHUB_TOKEN:?GITHUB_TOKEN must be set}
      GITHUB_USER: ${GITHUB_USER:?GITHUB_USER must be set (used as commit author)}
      GITHUB_EMAIL: ${GITHUB_EMAIL:?GITHUB_EMAIL must be set}
      PLATFORM_GIT_NAME: ${PLATFORM_GIT_NAME:-}
      PLATFORM_GIT_EMAIL: ${PLATFORM_GIT_EMAIL:-}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_ADMIN_CHAT_ID: ${TELEGRAM_ADMIN_CHAT_ID:-}
      PUBLIC_DOMAIN: ${PUBLIC_DOMAIN:?PUBLIC_DOMAIN must be set (e.g. yourdomain.com)}
      # The bind paths below MUST match between host and container so that when
      # the app asks the host's docker daemon to bind-mount a workspace dir into
      # a code-server container, docker can resolve the path on the host.
      WORKSPACE_ROOT: ${APP_HOST_DIR:-/srv/devplatform}/workspaces
      CONFLICT_CHECK_ROOT: ${APP_HOST_DIR:-/srv/devplatform}/conflict-check
      TRIGGERS_DIR: ${APP_HOST_DIR:-/srv/devplatform}/triggers
      DOCKER_HOST: unix:///var/run/docker.sock
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${APP_HOST_DIR:-/srv/devplatform}/workspaces:${APP_HOST_DIR:-/srv/devplatform}/workspaces
      - ${APP_HOST_DIR:-/srv/devplatform}/conflict-check:${APP_HOST_DIR:-/srv/devplatform}/conflict-check
      - ${APP_HOST_DIR:-/srv/devplatform}/triggers:${APP_HOST_DIR:-/srv/devplatform}/triggers
    ports:
      - "127.0.0.1:3000:3000"

volumes:
  db_data:
